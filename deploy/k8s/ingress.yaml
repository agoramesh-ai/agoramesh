# AgentMe Node Ingress
# HTTP API exposed via Ingress controller
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agentme-node
  namespace: agentme
  labels:
    app.kubernetes.io/name: agentme-node
    app.kubernetes.io/component: ingress
  annotations:
    # NGINX Ingress annotations
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"

    # Rate limiting at ingress level
    nginx.ingress.kubernetes.io/limit-rps: "50"
    nginx.ingress.kubernetes.io/limit-connections: "20"

    # CORS configuration
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-origin: "https://agentme.io"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization"

    # cert-manager for TLS
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - node.agentme.io
      secretName: agentme-node-tls
  rules:
    - host: node.agentme.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agentme-node
                port:
                  name: http
---
# Ingress for testnet
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: agentme-node-testnet
  namespace: agentme
  labels:
    app.kubernetes.io/name: agentme-node
    app.kubernetes.io/component: ingress
    environment: testnet
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"

    # Rate limiting at ingress level
    nginx.ingress.kubernetes.io/limit-rps: "50"
    nginx.ingress.kubernetes.io/limit-connections: "20"

    # CORS configuration
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-origin: "https://agentme.io"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-headers: "DNT,X-CustomHeader,Keep-Alive,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Authorization"

    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - testnet.node.agentme.io
      secretName: agentme-node-testnet-tls
  rules:
    - host: testnet.node.agentme.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: agentme-node
                port:
                  name: http
