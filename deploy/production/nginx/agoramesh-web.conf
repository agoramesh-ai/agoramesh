# Redirect bare domain to www
server {
    listen 80;
    server_name agoramesh.ai;
    return 301 https://www.agoramesh.ai$request_uri;
}

# Main website
server {
    listen 80;
    server_name www.agoramesh.ai;

    root /var/www/agoramesh;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
