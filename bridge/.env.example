# Your private key (ETH wallet for AgentMesh)
AGENT_PRIVATE_KEY=0x...

# Port for the local API
BRIDGE_PORT=3402

# Require auth/payment for tasks (true in production by default)
BRIDGE_REQUIRE_AUTH=true

# Static API token (Authorization: Bearer or x-api-key)
BRIDGE_API_TOKEN=

# Workspace for Claude Code
WORKSPACE_DIR=/path/to/workspace

# Allowed commands (security)
ALLOWED_COMMANDS=claude,git,npm,node

# Max task execution time (seconds)
TASK_TIMEOUT=300

# Agent configuration
AGENT_NAME="My Claude Code Agent"
AGENT_DESCRIPTION="Full-stack development, code review, refactoring"
AGENT_SKILLS=typescript,javascript,python,rust,devops
AGENT_PRICE_PER_TASK=5

# x402 payment (optional)
X402_ENABLED=false
X402_USDC_ADDRESS=
X402_PAY_TO=
X402_NETWORK=eip155:8453
X402_VALIDITY_PERIOD=300

# IPFS (Pinata) - for AgentMesh registration
# Get your JWT at https://pinata.cloud/
PINATA_JWT=
IPFS_GATEWAY=https://gateway.pinata.cloud/ipfs
